import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  registerWrapper: {
    // width: "70%",
  },
  authBtn: {
    width: "100%",
    padding: 5,
    borderRadius: 10,
    backgroundColor: "gray",
  },
});
